

disconnected mode:

input for server ip
input for pair code
button for connect

connected once but server unreachble / unhealthy:

same form (so user is able to change the info)
but a retry button to enforce/refresh the network call/connection
--> when device is cannot reach configured server for more than 5 minutes, display a persistent notification

connected healthy:

connect since button
a disconnect button
